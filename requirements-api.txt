# Dependencias mínimas para servir api/main.py (inferencia ONNX + landmarks).
# Subconjunto de requirements.txt — evita instalar torch/transformers/wandb/
# matplotlib de entrenamiento en la imagen Docker de producción.

torch>=2.1.0
opencv-python-headless>=4.8.0
mediapipe>=0.10.0
onnxruntime>=1.16.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.4.0
numpy>=1.24.0
